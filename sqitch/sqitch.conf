[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "postgres_dev"]
	uri = db:pg://astronauth:test@localhost:5432/astronauth
